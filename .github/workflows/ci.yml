name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: raid_team_stats
          POSTGRES_PASSWORD: ci_password
          POSTGRES_DB: raid_team_stats
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U raid_team_stats -d raid_team_stats"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 12

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 12

    env:
      NODE_ENV: test
      APP_URL: http://localhost:3000
      LOG_LEVEL: warn
      DATABASE_URL: postgresql://raid_team_stats:ci_password@localhost:5432/raid_team_stats?schema=public
      REDIS_URL: redis://localhost:6379
      AUTH_SECRET: Y2ktb25seS1hdXRoLXNlY3JldC1ub3QtZm9yLXByb2R1Y3Rpb24tdXNlLTEyMzQ1
      # base64("test-encryption-key-32bytes-aaaa") — decodes to exactly 32 bytes.
      TOKEN_ENCRYPTION_KEY: dGVzdC1lbmNyeXB0aW9uLWtleS0zMmJ5dGVzLWFhYWE=
      BLIZZARD_REGION: us
      WCL_HOURLY_POINTS_BUDGET: 17000
      SMTP_PORT: 587
      RATE_LIMIT_TRUST_PROXY: "false"
      # Skip the live-Redis token-bucket tests in CI for now; they'd need a
      # dedicated Redis cleanup step to avoid bleed between runs.
      SKIP_REDIS_TESTS: "1"
      NEXT_PUBLIC_APP_NAME: Raid Team Stats

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund --include=dev

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Apply migrations
        run: npx prisma migrate deploy

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Unit tests
        run: npm test --if-present

      - name: Build
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: 1
          # `next build` MUST run with NODE_ENV=production: React only loads its
          # production build when NODE_ENV==="production" (react/index.js); under
          # the job-level NODE_ENV=test it would load the DEV build, whose
          # dispatcher throws "useContext of null" while statically prerendering
          # /_global-error and /share under cacheComponents. Matches the
          # Dockerfile, which sets the same for its build stage.
          NODE_ENV: production
          # Build doesn't need live secrets — env.ts re-validates at boot.
          SKIP_ENV_VALIDATION: "1"

      - name: npm audit (production)
        run: npm audit --omit=dev --audit-level=high
        continue-on-error: true

  e2e:
    name: Playwright E2E smoke
    runs-on: ubuntu-latest
    needs: build
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: raid_team_stats
          POSTGRES_PASSWORD: ci_password
          POSTGRES_DB: raid_team_stats
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U raid_team_stats -d raid_team_stats"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 12

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 12

    # Job-level env is shared but NODE_ENV is deliberately omitted here so
    # npm install picks up devDependencies. The `Start app` step sets
    # production NODE_ENV when it actually matters.
    env:
      APP_URL: http://localhost:3000
      LOG_LEVEL: warn
      DATABASE_URL: postgresql://raid_team_stats:ci_password@localhost:5432/raid_team_stats?schema=public
      REDIS_URL: redis://localhost:6379
      AUTH_SECRET: Y2ktb25seS1hdXRoLXNlY3JldC1ub3QtZm9yLXByb2R1Y3Rpb24tdXNlLTEyMzQ1
      TOKEN_ENCRYPTION_KEY: dGVzdC1lbmNyeXB0aW9uLWtleS0zMmJ5dGVzLWFhYWE=
      BLIZZARD_CLIENT_ID: ci-noop
      BLIZZARD_CLIENT_SECRET: ci-noop
      BLIZZARD_REGION: us
      WCL_CLIENT_ID: ci-noop
      WCL_CLIENT_SECRET: ci-noop
      WCL_HOURLY_POINTS_BUDGET: 17000
      WCL_REDIRECT_URI: http://localhost:3000/wcl-callback
      SMTP_HOST: example.invalid
      SMTP_PORT: 587
      SMTP_USER: ci@example.invalid
      SMTP_PASSWORD: ci-noop
      SMTP_FROM: ci@example.invalid
      RATE_LIMIT_TRUST_PROXY: "false"
      NEXT_PUBLIC_APP_NAME: Raid Team Stats
      # Required by env.ts at runtime (gates /api/metrics). The started app
      # validates env (no SKIP_ENV_VALIDATION), so it must be present or
      # middleware throws "Invalid environment variables" and every route 5xx's.
      METRICS_TOKEN: ci-metrics-token-not-secret

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci --no-audit --no-fund --include=dev

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Apply migrations
        run: npx prisma migrate deploy

      - name: Build app
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: 1
          SKIP_ENV_VALIDATION: "1"

      - name: Start app
        run: |
          npm run start > app.log 2>&1 &
          for i in {1..40}; do
            curl -fsS http://localhost:3000/api/health && break
            sleep 1
          done
        env:
          NODE_ENV: production

      - name: Playwright smoke
        run: npm run test:e2e

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: playwright-report
          path: playwright-report
          if-no-files-found: ignore

      - name: Upload app log
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: app-log
          path: app.log
          if-no-files-found: ignore
